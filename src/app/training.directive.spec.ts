import { TrainingDirective } from './training.directive';

describe('TrainingDirective', () => {
  it('should create an instance', () => {
    const directive = new TrainingDirective();
    expect(directive).toBeTruthy();
  });
});
